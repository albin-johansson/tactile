#pragma once

namespace tactile {

/**
 * @brief Performs basic setup for the application.
 *
 * @details This function is meant to be called early during the construction
 * of an `app` instance.
 *
 * @see `app`
 *
 * @since 0.1.0
 */
void setup_app();

}  // namespace tactile
