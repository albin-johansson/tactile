[package]
name = "tactile"
repository.workspace = true
license.workspace = true
version.workspace = true
edition.workspace = true

[dependencies]
derive_more = { version = "2.0", features = ["add", "add_assign"] }
glam = "0.30"

[dev-dependencies]
googletest = "0.14"
